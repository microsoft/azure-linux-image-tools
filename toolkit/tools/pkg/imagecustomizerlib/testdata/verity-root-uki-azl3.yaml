previewFeatures:
- base-configs

baseConfigs:
- path: verity-root-uki-base.yaml

os:
  packages:
    remove:
    - grub2-efi-binary

    install:
    - openssh-server
    - veritysetup
    - systemd-boot
    - device-mapper
