previewFeatures:
- output-artifacts
- uki

os:
  bootloader:
    resetType: hard-reset
  uki:
    kernels: auto

input:
  image:
    path: testimages/empty-base.vhdx

output:
  image:
    path: ./out/output-image-1.vhdx
    format: vhdx
  artifacts:
    items:
    - ukis
    path: ./artifacts-1
