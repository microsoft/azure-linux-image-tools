# Ensure disk size is large enough for multiple kernels.
storage:
  resizeDisk:
    partitions:
    - ref: last
      freeSpace: 3G

os:
  kernelCommandLine:
    extraCommandLine:
    - console=tty0
    - console=ttyS0
