# Differs from selinux-policy-output in its use of selinux-policy-targeted (not selinux-policy-modules)
previewFeatures:
  - output-selinux-policy

os:
  packages:
    install:
    - selinux-policy
    - selinux-policy-targeted
    - setools-console
    - policycoreutils-python-utils

output:
  selinuxPolicyPath: selinux-output
