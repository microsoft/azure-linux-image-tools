# Differs from selinux-policy-output in its removal of selinux-policy-modules
previewFeatures:
  - output-selinux-policy

os:
  packages:
    install:
    - selinux-policy
    - setools-console
    - policycoreutils-python-utils

output:
  selinuxPolicyPath: selinux-output
