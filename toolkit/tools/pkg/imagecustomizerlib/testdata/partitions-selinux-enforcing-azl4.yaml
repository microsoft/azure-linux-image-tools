# Differs from partitions-selinux-enforcing-azl3 in its removal of selinux-policy-modules
previewFeatures:
- base-configs

baseConfigs:
- path: partitions-selinux-enforcing-base.yaml

os:
  packages:
    install:
    - selinux-policy
    - setools-console
    - policycoreutils-python-utils
