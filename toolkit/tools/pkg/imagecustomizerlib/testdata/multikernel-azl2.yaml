previewFeatures:
- base-configs

baseConfigs:
- path: multikernel-base.yaml

os:
  packages:
    installLists:
    - lists/multikernel-azl2.yaml

  kernelCommandLine:
    extraCommandLine:
    - console=tty0
    - console=ttyS0
