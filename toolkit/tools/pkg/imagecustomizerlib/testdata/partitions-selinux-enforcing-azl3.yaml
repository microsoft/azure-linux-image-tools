previewFeatures:
- base-configs

baseConfigs:
- path: partitions-selinux-enforcing-base.yaml

os:
  packages:
    install:
    - selinux-policy
    - selinux-policy-modules
    - setools-console
    - policycoreutils-python-utils
