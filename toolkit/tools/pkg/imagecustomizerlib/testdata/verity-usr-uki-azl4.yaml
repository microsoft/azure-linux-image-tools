# Differs from verity-user-uki-azl3 in that:
#
# - its boot partition is 1G
# - it uses systemd-boot-unsigned instead of systemd-boot
# - no post-customization script because iptables is not installed (it's been replaced with iptables-nft in azl4)
previewFeatures:
- uki

storage:
  bootType: efi
  disks:
  - partitionTableType: gpt
    partitions:
    - id: esp
      type: esp
      size: 250M

    - id: boot
      size: 1G

    - id: usr
      size: 1G

    - id: usrhash
      size: 100M

    - id: root
      size: 500M

    - id: var
      size: 1G

  verity:
  - id: verityusr
    name: usr
    dataDeviceId: usr
    hashDeviceId: usrhash
    corruptionOption: panic

  filesystems:
  - deviceId: esp
    type: fat32
    mountPoint:
      path: /boot/efi
      options: umask=0077

  - deviceId: boot
    type: ext4
    mountPoint: /boot

  - deviceId: root
    type: ext4
    mountPoint:
      path: /
      options: noexec

  - deviceId: verityusr
    type: ext4
    mountPoint:
      path: /usr
      options: ro

  - deviceId: var
    type: ext4
    mountPoint: /var

os:
  bootloader:
    resetType: hard-reset

  kernelCommandLine:
    extraCommandLine:
    - rd.info

  uki:
    mode: create

  packages:
    remove:
    - grub2-efi-binary

    install:
    - openssh-server
    - veritysetup
    - systemd-boot-unsigned
    - device-mapper
