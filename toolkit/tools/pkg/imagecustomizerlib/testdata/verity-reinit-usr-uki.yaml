previewFeatures:
- reinitialize-verity
- uki

storage:
  reinitializeVerity: all

os:
  kernelCommandLine:
    extraCommandLine:
    - rd.info

  bootloader:
    resetType: hard-reset

  uki:
    mode: create

  additionalFiles:
  - content: |
      Testing UKI re-customization
    destination: /etc/uki-test.txt

