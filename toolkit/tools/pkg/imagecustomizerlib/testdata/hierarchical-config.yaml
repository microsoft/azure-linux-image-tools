previewFeatures:
- output-artifacts
- base-configs

baseConfigs:
  - path: hierarchical-config-base.yaml

os:
  hostname: testname
  users:
    - name: test

input:
  image:
    path: testimages/empty.vhdx
  
output:
  image:
    path: ./out/output-image-2.vhdx
    format: vhdx
  artifacts:
    items:
    - shim
    path: ./artifacts-2
