# Differs from multikernel-azl3 in the kernel versions
previewFeatures:
- base-configs

baseConfigs:
- path: multikernel-base.yaml

os:
  packages:
    installLists:
    - lists/multikernel-azl4.yaml

  kernelCommandLine:
    extraCommandLine:
    - console=tty0
    - console=ttyS0
