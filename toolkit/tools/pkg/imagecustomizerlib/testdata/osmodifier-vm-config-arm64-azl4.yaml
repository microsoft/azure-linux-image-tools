# Differs from osmodifier-vm-config-arm64 its use of selinux-policy-targeted (not selinux-policy-modules
os:
  additionalFiles:
    # Enable DHCP client on all of the physical NICs.
  - source: files/89-ethernet.network
    destination: /etc/systemd/network/89-ethernet.network
  packages:
    install:
    - dracut-virtio
  packages:
    install:
    - selinux-policy
    - selinux-policy-targeted
    - setools-console
    - policycoreutils-python-utils
