# Differs from packagemanifest-create-remove-package-manager-azl3 in the pinned tree and dos2unix versions and its use of dnf instead of tdnf.
previewFeatures:
- base-configs

baseConfigs:
- path: packagemanifest-create-remove-package-manager-base.yaml

os:
  packages:
    install:
    - tree-0:2.2.1-3.azl4

scripts:
  postCustomization:
  - content: dnf --assumeyes install dos2unix-0:7.5.3-2.azl4
