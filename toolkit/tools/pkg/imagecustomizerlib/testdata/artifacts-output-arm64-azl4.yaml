# Differs from artifacts-output-amd64-azl4 in its use of grub2-efi-aa64 (not grub2-efi-x64)
storage:
  disks:
  - partitionTableType: gpt
    partitions:
    - id: esp
      type: esp
      size: 100M

    - id: boot
      size: 100M

    - id: rootfs
      size: 2G

  bootType: efi

  filesystems:
  - deviceId: esp
    type: fat32
    mountPoint:
      path: /boot/efi
      options: umask=0077

  - deviceId: boot
    type: ext4
    mountPoint:
      path: /boot

  - deviceId: rootfs
    type: ext4
    mountPoint:
      path: /

previewFeatures:
- uki
- output-artifacts

output:
  artifacts:
    items: 
    - ukis
    - shim
    - bootloader
    path: ./output

os:
  bootloader:
    resetType: hard-reset

  uki:
    mode: create

  packages:
    remove:
    - grub2-efi-aa64
    install:
    - systemd-boot-unsigned
    - openssh-server

  services:
    enable:
    - sshd
